name: Deploy to Aliyun

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install deps
        run: npm ci

      - name: Build Next.js
        run: npm run build

      - name: Prepare standalone package
        run: |
          mkdir -p deploy/.next
          cp -r .next/standalone/* deploy/
          cp -r .next/static deploy/.next/
          cp -r public deploy/

      - name: Restore SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.ALI_SSH_KEY_B64 }}" | base64 -d > ~/.ssh/aliyun_key
          chmod 600 ~/.ssh/aliyun_key
          ssh-keyscan -p ${{ secrets.ALI_PORT }} ${{ secrets.ALI_HOST }} >> ~/.ssh/known_hosts

      - name: Copy files to Aliyun
        run: |
          scp -P "${{ secrets.ALI_PORT }}" -i ~/.ssh/aliyun_key -r deploy/* "${{ secrets.ALI_USER }}@${{ secrets.ALI_HOST }}:${{ secrets.ALI_DEPLOY_PATH }}/"

      - name: Restart Node project
        run: |
          ssh -p "${{ secrets.ALI_PORT }}" -i ~/.ssh/aliyun_key "${{ secrets.ALI_USER }}@${{ secrets.ALI_HOST }}" '
            cd "${{ secrets.ALI_DEPLOY_PATH }}"
            mkdir -p .next/standalone/.next
            cp -r -f .next/static .next/standalone/.next/
            cp -r -f public .next/standalone/
            pkill -f "node ./.next/standalone/server.js" || true
            nohup sh -c '"'"'HOSTNAME=0.0.0.0 PORT=3000 node ./.next/standalone/server.js'"'"' > app.log 2>&1 &
          '